@using MyHealthBlog.Domain
@model FoodObject

<div class="col-sm-4 col-lg-4 col-md-4">
    <h4>@Model.Name</h4>
    <div class="thumbnail">
        <img src="@Model.ImageUrl" alt="">
        <div>
            <p class="button">
                <a class="btn btn-primary" asp-controller="Food" asp-action="UpdateForm"
                   asp-route-id="@Model.Id">Update</a>
            </p>
        </div>
    </div>
</div>
